{
  "name": "@nexusforge/desktop",
  "version": "1.0.0",
  "private": true,
  "main": "main.js",
  "scripts": {
    "dev": "wait-on tcp:3000 && electron .",
    "dev:full": "wait-on tcp:3000 tcp:4000 && electron ."
  },
  "devDependencies": {
    "electron": "^42.0.1",
    "png-to-ico": "^3.0.1",
    "wait-on": "^8.0.5"
  }
}
